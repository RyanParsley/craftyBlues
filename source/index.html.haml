---
pageable: true
per_page: 5
layout: blog
title: Articles
selected: blog
---

- page_articles.each_with_index do |article, i|
  %article
    = link_to article do 
      %h2
        =article.title
    %span.date= article.date.strftime('%b %e')
    = article.summary
    =link_to 'Read the whole post', article, :class => "readMore"
- if paginate && (prev_page || next_page)
  .pagers
    - if prev_page
      = link_to 'Newer posts', prev_page, :class => "previous"
    - if next_page
      = link_to 'Older posts', next_page, :class => "next"
