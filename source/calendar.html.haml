---
pageable: true
layout: calendar
---

%ul
  - page_articles.each_with_index do |article, i| 
    %li
      = link_to article.title, article
      %span
        = article.date.strftime('%b %e')

- if paginate && (prev_page || next_page)
  .pagers
    - if prev_page
      = link_to 'Newer posts', prev_page, :class => "previous"
    - if next_page
      = link_to 'Older posts', next_page, :class => "next"

